def compute test(double a, double b)
{
return (a + b);
}


def compute main()
{
int x = 3;
double y = 2.0;
double r = 0;
r = test(x, y); # int can be used in a place of a double. 
print(r);

}

